- if @has_authorized_services
  .dashboard.row
    .activities.col-md-12
      = render 'shared/event_star_filter'
      - if @events.any?
        .content_list
      - else
        .nothing-here-block
          Your favorite content will appear here, but now it is zero.

      = spinner

- else
  = render "zero_authorized_projects"
