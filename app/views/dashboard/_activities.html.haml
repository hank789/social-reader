= render "events/event_last_unread"
= render 'shared/event_filter'

- if @events.any?
  .content_list
- else
  .nothing-here-block
    No service activity currently at
    = @event_filter.show_filter

= spinner
