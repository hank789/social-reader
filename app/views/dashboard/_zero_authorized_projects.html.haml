%h3.page-title Welcome to poqbod!
%hr
%div
  .dashboard-intro-icon
    %i.icon-bookmark-empty
  %div
    %p.slead
      You don't have access to any service right now.
      %br
      - if current_user.can_create_project?
        You can create up to
        %strong= pluralize(current_user.projects_limit, "service") + "."
        Click on the button below to add a new one
      - else
        If you are added to a project, it will be displayed here

    - if current_user.can_create_project?
      .link_holder
        = link_to new_service_path, class: "btn btn-new" do
          New service »
